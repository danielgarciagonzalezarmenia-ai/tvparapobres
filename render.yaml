services:
  - type: web
    name: tv-para-pobres
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/proxy.js
    healthCheckPath: /health
    envVars:
      - key: PC_BACKEND_URL
        sync: false
        value: ""